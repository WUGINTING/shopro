# 生產環境配置
ENV = 'production'

# 生產環境 API 位址：預設 /api（由網站伺服器反向代理到後端，同網域不需 CORS）
# 若後端在其他網域，建置時覆寫：VITE_APP_BASE_API=https://api.example.com/api npx quasar build
# （並將前台網址加入後端 CORS_ALLOWED_ORIGINS）
VITE_APP_BASE_API = /api

# 生產環境讀取設定檔路徑
VITE_PUBLIC_PATH = /

# Google Analytics
VITE_APP_GA_ID = ''

spring.application.name=E-commerce

# === H2 Database for testing ===
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# === JPA/Hibernate ===
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# === JWT Configuration for testing ===
# 測試專用密鑰，請勿在生產環境使用
jwt.secret=dGVzdC1vbmx5LXNlY3JldC1rZXktZG8tbm90LXVzZS1pbi1wcm9kdWN0aW9uLTEyMzQ1Njc4
# (base64 of "test-only-secret-key-do-not-use-in-production-12345678")
jwt.expiration=86400000

# === File Upload ===
file.upload-dir=target/test-uploads

# === Payment Gateway (Test Mode) ===
payment.linepay.channel-id=test-channel
payment.linepay.channel-secret-key=test-secret
payment.linepay.sandbox=true

payment.ecpay.merchant-id=test-merchant
payment.ecpay.hash-key=test-hash-key
payment.ecpay.hash-iv=test-hash-iv
payment.ecpay.sandbox=true

# === App URLs ===
app.base-url=http://localhost:8080
app.frontend-url=http://localhost:5173

# === Rate limits（整合測試大量下單，放寬限制；限流本身另有測試） ===
app.rate-limit.checkout-per-client=10000
app.rate-limit.checkout-per-email=10000
